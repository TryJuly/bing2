# Minishell
Write a shell

Ex command:
-	echo "salut
-	echo "sa'lut"

- 	echo "'salut'"
- 	echo ""
-	echo ''
- 	echo "		"
-	echo "sa"lut

-	echo $USER$PATH
-	echo "$USER"
-	echo '$USER'
-	echo $UNDEFINED
-	echo "$?"
-	echo a$?b

-	| ls
-	ls || wc
-	ls | | wc
-	ls |
-	ehco test |grep t

< log.txt cat | grep "error" | wc > error.txt -l
